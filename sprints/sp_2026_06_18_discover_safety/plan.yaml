sprint_id: sp_2026_06_18_discover_safety
repo: 03hoho03/ai-character
milestone: sp_2026_06_18_discover_safety
milestone_number: 3
created_at: 2026-06-18
created_by: sprint-pm
length: 2weeks

sprint_goal: |
  공개 캐릭터를 검색·태그로 발견할 수 있게 해 B2에서 연 공유 루프를 완성하고(#17 검색·태그),
  발견으로 공개 노출이 늘기 전 서버측 안전 집행(클라 신뢰 부채 해소)과 성인/일반 등급분리를
  선결한다(#19). benchmark B3(탐색)·B5(안전)를 메우는 sprint. 인증은 cut 유지(익명 browserId).

# === 강제 게이트: out_of_scope ≥ 2 ===
out_of_scope:
  - "17c 인기 랭킹 — Conversation.personaId가 Character.id에 FK 없이 tpl-* 템플릿 id와 한 컬럼 공유 → groupBy 집계 시 고아·혼입 카운트 리스크. 별도 cycle로 다음 sprint(검색+태그로 발견 루프는 닫힘)."
  - "#22 사용자 기억 직접 편집 UI — sprint goal(발견·공유) 무관 retention 기능. + summary가 단일 String 필드라 '항목 단위 편집' 요구와 모델 불일치, 자동요약 덮어쓰기 충돌 정책 선결 필요."
  - "#20 멀티모달(대화→이미지) — 그린필드 + 이미지 모델·저장소·비용 미결정 외부 의존. 텍스트 전용 파이프라인 전면 확장. 스파이크 선행 후 별도 sprint."
  - "인증/회원 시스템 — 익명 browserId 유지. 공유·발견 자리잡은 뒤 별도 과제."
  - "추천 알고리즘/큐레이션 레일, 음성(TTS/STT), 그룹채팅, 수익화 — 코어 이후."

# === 이번 sprint 실행 ticket (4) ===
# 실행 순서: 19a(#23) → 17a(#24) → 17b(#25) → 19b(#26)
included:
  - issue: 23   # 19a
    parent: 19
    title: "안전 집행 — 서버측 instruction 재조립/검증 (클라 신뢰 부채 해소) (#19 1/2)"
    estimated_size: M
    order: 1
    pm_view:
      urgency: high
      value: "발견으로 공개 노출이 늘기 직전이 안전 집행의 정확한 타이밍. 미집행 시 #17 노출이 곧 리스크."
      dependency: []
    arch_view:
      blast_radius: "chat.service.ts/chat.controller.ts, persona-prompt.ts(서버 이동 시 호출부 전이), useChatStream.ts(클라 조립 제거), chat.constants(safetySettings)"
      risk: "instruction이 클라에서 와 서버 무검증 신뢰 — 서버 집행 미추가 시 prohibition·등급 클라 우회 가능(이 우회 통로가 진짜 대상). chat 핵심 경로/#13 규약 회귀 위험."
      decomposed_from: "#19 → 19a 집행 / 19b 등급분리"
    user_break: "사용자 결정 — 19a 집행을 #17보다 먼저(공개 노출 전 선결)."

  - issue: 24   # 17a
    parent: 17
    title: "캐릭터 검색 — 공개 캐릭터 키워드 검색 (#17 1/3)"
    estimated_size: S~M
    order: 2
    pm_view:
      urgency: high
      value: "발견 경험 MVP의 코어. 평면 카드 목록 → 검색으로 공유 루프를 닫는 첫 조각."
      dependency: [23]
    arch_view:
      blast_radius: "characters.service.ts/controller.ts(GET /characters/public ?q=), dto, characters-api.ts/character-store.ts, user-character-list.tsx"
      risk: "Prisma contains — 마이그레이션 불필요. 낮음. 신규 query param은 HTTP 레벨 테스트 필요(lesson)."
      decomposed_from: "#17 → 17a 검색 / 17b 태그 / 17c 랭킹"
    user_break: "사용자 결정 — 검색+태그 include, 랭킹 defer."

  - issue: 25   # 17b
    parent: 17
    title: "캐릭터 태그/카테고리 — 분류 컬럼 + 생성·편집 입력 + 필터 (#17 2/3)"
    estimated_size: M
    order: 3
    pm_view:
      urgency: high
      value: "검색과 함께 발견 루프를 닫음. 태그 필터가 발견 경험의 절반."
      dependency: [24]
    arch_view:
      blast_radius: "schema.prisma(Character.tags/category + 마이그레이션 + 인덱스), characters.service/dto, 생성·편집 폼, public 필터 쿼리, shared(Persona/CharacterRecord)"
      risk: "컬럼 추가 마이그레이션 + 폼 결합. 중간. HTTP 레벨 테스트 필요."

  - issue: 26   # 19b
    parent: 19
    title: "콘텐츠 등급 — 성인/일반 분리 (Character.contentRating) + 공개목록 필터 (#19 2/2)"
    estimated_size: S~M
    order: 4
    pm_view:
      urgency: medium
      value: "공개 발견에 성인/일반 분리 적용 — 안전 게이트의 노출측 절반."
      dependency: [25, 23]
    arch_view:
      blast_radius: "schema.prisma(Character.contentRating + 마이그레이션), 생성·편집 폼 플래그, public 목록(#17) 필터링"
      risk: "17b 태그와 동일 마이그레이션·폼 결합점 — 함께 처리 시 충돌 완화. 낮음."

# === deferred (issue 유지, Milestone 미할당) ===
deferred:
  - slice: "17c 인기 랭킹"
    parent_issue: 17
    reason: "Conversation.personaId FK 부재 + 템플릿 id 혼입으로 집계 신뢰성 리스크. 별도 cycle. 검색+태그로 발견 루프는 닫힘."
    revisit_after: "#24, #25"
  - issue: 22
    title: "사용자 기억 직접 편집 UI"
    reason: "sprint goal 무관. summary 단일 String vs 항목 편집 요구 불일치, 자동요약 덮어쓰기 충돌 정책 선결."
    revisit_after: null
  - issue: 20
    title: "멀티모달 — 대화→이미지 생성"
    reason: "그린필드 + 모델·저장소·비용 미결정 외부 의존. 비용/모델 스파이크 선행 필요."
    revisit_after: "비용·모델 스파이크"

cut: []  # cut 없음 — 전량 defer로 추적 유지

# === acceptance gate ===
gates:
  normalization_accuracy: "기존 issue 분해, 사용자 break 100% confirm"   # ✓
  out_of_scope_count: 5          # ≥2 ✓
  deferred_ratio: "3/7 work-units = 43%"  # cut+deferred ≥30% ✓ (17a/17b/19a/19b include, 17c/#22/#20 defer)
  pm_arch_conflicts: 2           # #17 분해 granularity(2 vs 3분할·랭킹 포함 여부), #19 시퀀싱(#17 앞 vs 병행)  ≥1 ✓
  user_attestation: attested  # 2026-06-18 사용자 승인
