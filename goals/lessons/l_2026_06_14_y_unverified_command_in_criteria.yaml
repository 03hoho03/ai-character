lesson_id: l_2026_06_14_y_unverified_command_in_criteria
category: define_weakness
title: "PRD 성공 기준이 실존 미확인 명령을 가정 — `shared build` 적었으나 build 스크립트 부재"
created_at: "2026-06-14"
updated_at: "2026-06-14"

sources:
  - run_id: dev-cycle/2026_06_13_character-crud-backend (비정식 — verify_report 기반 회고)
    file_ref: "dev-cycle/2026_06_13_character-crud-backend/verify_report.md#iteration-1-notes-1"
    quote: |
      PRD 성공 기준이 문자 그대로 `shared build`를 적었으나 해당 패키지에 `build` 스크립트
      없음 → 구현 결함 아님(명령 불일치). `typecheck`로 동치 검증.
    observed_at: "2026-06-14"

n_cycles: 1

pattern:
  trigger: "PRD Phase에서 성공 기준에 `pnpm --filter X <script>` 형태 명령을 적되, 해당 package.json scripts에 그 스크립트가 실존하는지 확인하지 않음"
  symptom: "verify 단계에서 명령이 존재하지 않아, 독립 verifier가 동치 명령(typecheck)으로 임의 대체해 PASS 판정"
  failure_mode: "성공 기준이 문자 그대로는 실행 불가 → 검증의 객관성 약화(검증자의 즉석 동치 판단이 개입). 다른 패키지였다면 가짜 PASS로 샐 여지"

seed_taxonomy_match: weak_check

confidence: tentative
confidence_reason: |
  n=1 (이번 세션 #16 단발). executor가 적은 기준 vs 실제 scripts 대조로 확인 — tentative.

counter_evidence: []

proposed_patch:
  target: "next_goal_context"
  kind: "text_change"
  diff_hint: |
    PRD 성공 기준에 실행 명령을 적을 때, 그 명령(특히 `build`/`lint`/커스텀 script)이
    대상 package.json의 scripts에 실존하는지 먼저 확인. 없으면 실존하는 명령(typecheck/test)으로
    기준을 적는다. 검증자가 기준을 즉석에서 재정의하게 만들지 말 것.

  applied: false
  applied_at: ""
  applied_by: ""

applies_to_next_goal:
  - "다음 dev-cycle Phase 1에서 성공 기준의 명령을 package.json scripts와 대조"

remaining_ambiguity:
  - "이번 monorepo는 패키지마다 scripts가 달라 발생 — 단일 패키지 프로젝트에선 빈도 낮을 수 있음"

not_actionable_reason: ""

metadata:
  created_by: "learn skill"
  learn_version: "v1"
  isolated_from_verify: false
  isolation_caveat: |
    verify와 같은 세션. 독립 verifier가 적발해 기록했으나, learn도 같은 세션이라 confidence 보수적으로.
