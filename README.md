# ai-character

AI 캐릭터 챗 웹 서비스 — Next.js (`apps/web`) + NestJS (`apps/api`) + 공용 타입 (`packages/shared`) pnpm 모노레포.

## 실행

```bash
pnpm install
pnpm dev   # web: http://localhost:3000 / api: http://localhost:4000/health
```
